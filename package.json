{
  "name": "spot-now-playing-service",
  "version": "1.0.0",
  "description": "Stateless, read-only Dockerized Spotify now playing service.",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
